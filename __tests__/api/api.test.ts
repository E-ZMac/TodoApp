import React from 'react'
import { render } from '@testing-library/react'

// describe('Home Page', () => {
    
//     it('rendersx home page', async () => {
//         const { getByText } = render(<Home />)
    
//         expect(getByText(/things to do/i)).toBeDefined()
//     })
// })